import{getAuth}from'https://www.gstatic.com/firebasejs/12.18.0/firebase-auth.js';import{getFirestore,collection,query,where,getDocs,updateDoc,deleteDoc}from'https://www.gstatic.com/firebasejs/12.18.0/firebase-firestore.js';import{getApps}from'https://www.gstatic.com/firebasejs/12.18.0/firebase-app.js';
const ADMIN='aBYdbFwbsTUbpmYtoNurxyu3Roj2',app=getApps()[0],auth=getAuth(app),db=getFirestore(app);
const style=document.createElement('style');style.textContent='.people-actions{display:flex;gap:6px;flex-wrap:wrap}.people-actions .pa{border:0;border-radius:9px;padding:7px 9px;font-size:10px;font-weight:900;cursor:pointer}.people-actions .toggle{background:#e8f6ee;color:#08783f}.people-actions .remove{background:#fff0ef;color:#b42318}.people-actions .pa:disabled{opacity:.55;cursor:wait}';document.head.appendChild(style);
async function findDoc(type,email){const q=await getDocs(query(collection(db,type),where('email','==',email)));return q.docs[0]||null}
function refresh(){const b=[...document.querySelectorAll('[data-nav]')].find(x=>x.dataset.nav==='people');if(b)b.click()}
async function action(type,email,op,button){if(auth.currentUser?.uid!==ADMIN)return;button.disabled=true;try{const d=await findDoc(type,email);if(!d){alert('Record not found.');return}const label=type==='students'?'Student':'Teacher';if(op==='remove'){if(!confirm(`Remove this ${label} from CREDENCE?`))return;await deleteDoc(d.ref);alert(`${label} removed.`)}else{const next=d.data().active===false;await updateDoc(d.ref,{active:next,updatedAt:new Date()});alert(`${label} ${next?'enabled':'disabled'}.`)}refresh()}catch(e){console.error(e);alert(e.message)}finally{button.disabled=false}}
function enhance(){if(auth.currentUser?.uid!==ADMIN)return;const people=[...document.querySelectorAll('[data-nav]')].find(x=>x.dataset.nav==='people');if(!people||!people.classList.contains('on'))return;document.querySelectorAll('.table').forEach(table=>{const heads=[...table.querySelectorAll('thead th')].map(x=>x.textContent.trim());const type=heads.includes('Class / Roll')?'students':heads.includes('Subject / Class')?'teachers':null;if(!type)return;if(!table.querySelector('thead .people-actions-head'))table.querySelector('thead tr')?.insertAdjacentHTML('beforeend','<th class="people-actions-head">Actions</th>');table.querySelectorAll('tbody tr').forEach(row=>{if(row.querySelector('.people-actions'))return;const cells=row.querySelectorAll('td');const email=cells[1]?.textContent.trim();const status=cells[3]?.textContent.trim().toLowerCase();if(!email||email==='—'||!cells[3])return;const td=document.createElement('td');td.innerHTML=`<div class="people-actions"><button class="pa toggle">${status==='disabled'?'Enable':'Disable'}</button><button class="pa remove">Remove</button></div>`;row.appendChild(td);td.querySelector('.toggle').onclick=()=>action(type,email,'toggle',td.querySelector('.toggle'));td.querySelector('.remove').onclick=()=>action(type,email,'remove',td.querySelector('.remove'))})})}
new MutationObserver(enhance).observe(document.body,{childList:true,subtree:true});setTimeout(enhance,500);setInterval(enhance,1200);