name: Load CREDENCE mobile reference UI v3

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy UI patch workflow disabled; final UI is managed by enable-enhancements.yml"
