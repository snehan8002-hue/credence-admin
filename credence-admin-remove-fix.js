import{getApps}from'https://www.gstatic.com/firebasejs/12.18.0/firebase-app.js';import{getAuth}from'https://www.gstatic.com/firebasejs/12.18.0/firebase-auth.js';import{getFirestore,doc,updateDoc}from'https://www.gstatic.com/firebasejs/12.18.0/firebase-firestore.js';
const ADMIN='aBYdbFwbsTUbpmYtoNurxyu3Roj2',app=getApps()[0],auth=getAuth(app),db=getFirestore(app);
document.addEventListener('click',async e=>{const b=e.target.closest?.('[data-remove-admin]');if(!b||auth.currentUser?.uid!==ADMIN)return;e.preventDefault();e.stopPropagation();e.stopImmediatePropagation();const uid=b.dataset.removeAdmin;if(!uid||uid===ADMIN)return;if(!confirm('Remove this admin from CREDENCE? They will no longer have admin access.'))return;b.disabled=true;try{await updateDoc(doc(db,'superAdmins',uid),{active:false});const row=b.closest('.admin-row');if(row){const s=row.querySelector('span');if(s)s.textContent=(s.textContent||'').replace(/·\s*(Active|Disabled)$/,'· Disabled');b.textContent='Removed';b.disabled=true;b.style.opacity='.65'}alert('Admin removed from CREDENCE successfully.')}catch(x){console.error(x);b.disabled=false;alert(x.message||'Could not remove admin.')}},true);