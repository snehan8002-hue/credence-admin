const css=document.createElement('style');css.textContent='.ctf-hidden-pending{display:none!important}';document.head.appendChild(css);function page(){return[...document.querySelectorAll('[data-nav].on')].map(x=>x.dataset.nav)[0]||''}function hidePendingOutsideHome(){const home=page()==='home';if(home){document.querySelectorAll('.ctf-hidden-pending').forEach(x=>x.classList.remove('ctf-hidden-pending'));return}const needles=['pending registration','pending registrations','registration requests','pending requests'];document.querySelectorAll('#view *').forEach(el=>{if(el.children.length>6)return;const t=(el.textContent||'').replace(/\s+/g,' ').trim().toLowerCase();if(!t||!needles.some(n=>t.includes(n)))return;let target=el.closest('.card,.section,.setting');if(!target){target=el.parentElement;while(target&&target.parentElement&&target.parentElement.id!=='view'&&target.children.length<=8)target=target.parentElement}if(target)target.classList.add('ctf-hidden-pending')})}function run(){hidePendingOutsideHome()}new MutationObserver(run).observe(document.body,{childList:true,subtree:true});setInterval(run,500);run();