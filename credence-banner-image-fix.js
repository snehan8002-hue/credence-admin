import{getApps}from'https://www.gstatic.com/firebasejs/12.18.0/firebase-app.js';import{getAuth,onAuthStateChanged}from'https://www.gstatic.com/firebasejs/12.18.0/firebase-auth.js';import{getFirestore,doc,getDoc}from'https://www.gstatic.com/firebasejs/12.18.0/firebase-firestore.js';const app=getApps()[0],auth=getAuth(app),db=getFirestore(app);let user=false,busy=false;async function fix(){if(!user||busy||typeof window.CREDENCE_STORAGE_SIGNED_URL!=='function')return;const box=document.querySelector('.cbx');if(!box)return;busy=true;try{const s=await getDoc(doc(db,'system','customization'));if(!s.exists())return;const a=(Array.isArray(s.data().banners)?s.data().banners:[]).filter(x=>x&&x.active!==false).sort((x,y)=>(+x.order||0)-(+y.order||0));const slides=box.querySelectorAll('.cbx-slide');for(let i=0;i<a.length;i++){const b=a[i],slide=slides[i];if(!slide)continue;const p=b.imagePath||b.storagePath;let u='';try{u=p?await window.CREDENCE_STORAGE_SIGNED_URL(p):(/^https?:\/\//i.test(b.image||'')?b.image:'')}catch{}if(!u)continue;let im=slide.querySelector('img');if(!im){im=document.createElement('img');slide.prepend(im)}im.src=u;im.alt=b.title||'CREDENCE banner';im.loading='eager';im.decoding='async';im.onerror=()=>setTimeout(fix,500)}}finally{busy=false}}onAuthStateChanged(auth,u=>{user=!!u;if(user){setTimeout(fix,600);setInterval(fix,30000)}});new MutationObserver(()=>setTimeout(fix,100)).observe(document.body,{childList:true,subtree:true});